namespace _Project.Dialogue
{
    public enum DialogueParserType
    {
        /// <summary>
        /// Run all parser rules/substitutions when the dialogue is loaded from files
        /// TODO: REword and rename
        /// </summary>
        OnLoad,
    }
}